import { TwodecimalpointDirective } from './twodecimalpoint.directive';

describe('TwodecimalpointDirective', () => {
  it('should create an instance', () => {
    const directive = new TwodecimalpointDirective();
    expect(directive).toBeTruthy();
  });
});
