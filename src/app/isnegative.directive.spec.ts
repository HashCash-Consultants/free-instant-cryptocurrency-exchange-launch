import { IsnegativeDirective } from './isnegative.directive';

describe('IsnegativeDirective', () => {
  it('should create an instance', () => {
    const directive = new IsnegativeDirective();
    expect(directive).toBeTruthy();
  });
});
